---
description: OpenGet — Human Verification / Trust-as-a-Service
globs: "**/*"
---

# OpenGet

- **Product:** open-source **Human Verification** — **7-factor** Kinetic scores (vaulted raw internally), public profiles, **SVG badges**, **/api/verify** JSON, optional **B2B talent** API, **Guardian** attestations. Emphasize **F3, F5, F6** in UX (merge, review, triage).
- **Never** add LOC to the main merit score. `lines_*` in DB are analytics only.
- Prefer **serverless** Appwrite functions + on-demand work; keep **low COGS**.
- See root **CONTEXT.md** for product context.
